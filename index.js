'use strict';

/**
 * Takes a string of plain text and converts it to a string with emojis
 * @param {string} game
 * @param {string} move
 * @return {string}
 */
module.exports = function(game, move) {
  return ;
};
